export default {
  queryPreprocess(root, args, context, ast) {
    //
  },
  queryMiddleware(queryPacket, root, args, context, ast) {
    //
  }
  Type2: {
    queryPreprocess(root, args, context, ast) {
      //
    },
    queryMiddleware(queryPacket, root, args, context, ast) {
      //
    } 
  }
};
